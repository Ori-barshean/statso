# כללי פרויקט — statso

הכללים כאן חלים על העבודה בריפו הזה. במקומות שמסומנים כ"מבטל" הם גוברים על ההוראות
הגלובליות ב-`~/.claude/CLAUDE.md`.

## מה הפרויקט

**statso** ([Ori-barshean/statso](https://github.com/Ori-barshean/statso), ציבורי) הוא מרכז
משיכת נתונים שמזין פרויקטים אחרים (CePee, Excel) בנתונים כלכליים ישראליים מנורמלים, דרך
כתובות `raw.githubusercontent.com` יציבות. מקור אמת אחד, במקום שכל פרויקט יממש מחדש קריאות
ל-API של הלמ"ס ובנק ישראל.

## 1. מספור גרסאות — X.Y.Z, החל מ-1.0.0

X = הרחבה, Y = גרסה, Z = תיקון.

- כל שינוי בפועל מעלה את ה-**patch אוטומטית, בלי לשאול**.
- העלאת X או Y — רק בהוראה מפורשת של אורי.
- הגרסה יושבת בקובץ `VERSION` בלבד, ובמכוון **לא** בשום מקום תחת `data/` — bump שם היה
  כותב מחדש את כל קבצי הדאטה ושובר אידמפוטנטיות.
- אין למספר הגרסה עותק שני בשום מקום: `index.html` מושך אותו בזמן ריצה מ-`VERSION`
  (`#version-source` → `Statso.data.loadVersion`), והבילד האופליין מטמיע אותו בזמן build
  דרך המרקר `data-inline="text"`. bump = לערוך את `VERSION` ולהריץ מחדש את הבילד.

מבטל את סכמת ה-0.001 הגלובלית עבור הפרויקט הזה.

## 2. Commits — לא צריך אישור

מבצעים commit על כל שינוי קוד תוך כדי העבודה. ה-GitHub Action הלילי גם הוא עושה auto-commit
לשינויים תחת `data/`.

## 3. Push / דיפלוי — חסום, בכל פעם מחדש

רק כשאורי כותב מפורשות **"דיפלוי"** או **"פרסם"**. בפרויקט הזה `git push` עצמו נחשב דיפלוי,
כי דחיפה ל-`main` היא מה שהופך את הדאטה ואת ה-UI לחיים בכתובות ה-raw/Pages הציבוריות.

**commit ≠ push.**

## 4. ניתוב כלי כתיבה לפי מורכבות

- כתיבה פשוטה מאוד → קלוד ישירות.
- כל דבר מורכב יותר → skill `/cct` אוטומטית, בלי שאורי יבקש.

מבטל את כלל 4.5 הגלובלי (Codex כברירת מחדל) עבור הפרויקט הזה.

## 5. לוח רענון הנתונים — יומי ב-02:00 שעון ישראל

שני הפידים מתרעננים כל יום, לא רק בתאריכי פרסום רשמיים: בנק ישראל מפרסם בתאריכים מוכרזים
מראש, והלמ"ס מזיזה את פרסום המדד של ה-15 כשהוא נופל בשישי/שבת/חג.

cron של GitHub הוא UTC בלבד, ולכן זה שתי כניסות cron — `0 0 * * *` (חורף) ו-`0 23 * * *`
(קיץ) — plus בדיקת `Asia/Jerusalem` בתוך ה-job שמדלגת על הלא-נכונה (`scripts/schedule_guard.py`).

## 6. בילד מבחן מקומי — תמיד זמין בשולחן העבודה

`~/Desktop/statso-test.html` — קובץ שנפתח בדאבל-קליק, **בלי שרת ובלי רשת**: הדאטה מוטמעת
בתוכו בזמן ה-build (`scripts/build_offline.py`).

- מתרענן אחרי **כל** שינוי ב-UI או בדאטה — כולל שינוי שביצע subagent.
- מאומת בפועל: קוראים את חותמת הגרסה בתוך הקובץ ומשווים ל-`VERSION`. לא מניחים שהבילד קרה.
- מנותק לגמרי מדיפלוי: הקובץ מציג את המצב המקומי החדש ביותר גם כשכלום לא נדחף.

## 7. אין גיבוי ב-OneDrive

לא יוצרים ולא מתחזקים תיקיית פרויקט ב-OneDrive, לא `קישורים.md`, ולא תיקיית `גרסאות/` עם
snapshots. git/GitHub הם מנגנון ההיסטוריה והגיבוי היחיד כאן.

מבטל את סעיפים 2–3 הגלובליים עבור הפרויקט הזה.

## 8. כללים גלובליים שעדיין חלים

- **graphify** — לקרוא את `graphify-out/GRAPH_REPORT.md` לפני כל חיפוש קוד; להריץ
  `/graphify . --update` (תמיד עם `--update`, לעולם לא `/graphify` רגיל) רק במעבר לגרסה
  שלמה חדשה.
- `graphify-out/` **לא נכנסת לגיט אף פעם** — פלט מיוצר; היא ב-`.gitignore`.
- **cct** — כל בקשת קוד בעברית עוברת בצינור 6 השלבים.
- כלל 4 הגלובלי (קיצור סביבת מבחן בשולחן העבודה) מיושם כאן דרך כלל 6 לעיל.

## מקורות הנתונים — endpoints מאומתים

אומתו בקריאות חיות ב-2026-09-05. **אין לחקור אותם מחדש.**

### מדד המחירים לצרכן — למ"ס, היסטוריה מ-9/1951

```
https://api.cbs.gov.il/index/data/price?id=120010&format=json&download=false&coef=true&pagesize=1000
```

`coef=true` הוא קריטי: הוא גורם לכל שורה לשאת את ערך אותו חודש כשהוא כבר מבוטא בכל בסיס
ישן. כך שרשור סדרה לבסיס אחד הוא **lookup, לעולם לא אריתמטיקה של מקדמים**.

### ריבית בנק ישראל — SDMX, סדרה `MNT_RIB_BOI_D`, היסטוריה מ-1994-01-27

```
https://edge.boi.gov.il/FusionEdgeServer/sdmx/v2/data/dataflow/BOI.STATISTICS/BR/1.0/MNT_RIB_BOI_D..?format=csv
```

- ה-`..` בסוף נושא משקל — מפתח `all` מחזיר HTTP 404.
- לא 1/1990: ל-API הרשמי אין שום דבר מוקדם יותר. אורי אישר את 1994 ב-2026-09-05.
- `https://boi.org.il/PublicApi/GetInterest` מחזיר רק את הערך הנוכחי + תאריך ההחלטה הבאה.
